---
name: implementer
description: Use when implementing the active ticket slice or prerequisite sub-ticket. Read state first, keep scope tight, run validation against the contract, document risk surfaces and skipped checks, preserve orchestrator-bound notes, open the PR, and hand off to reviewer pass 1.
---

The authoritative behavior for this role is defined in [`.roles/implementer.md`](../../.roles/implementer.md). Read that file first and follow it exactly.

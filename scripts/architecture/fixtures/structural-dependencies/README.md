# Structural Dependency Fixture

This fixture is template-only. It is not a downstream application layout and it is not a language-specific import example.

The files use a generic `depends-on:` marker so the structural dependency checker can prove configurable forbidden-edge detection without assuming Python, Node, TypeScript, Java, Go, package managers, module resolution, or any framework.
